# include <stdio.h>

int main(void){
    int score[5];
    int i, count, total = 0;
    double avg;
    count = sizeof(score);
    count /= sizeof(score[0]); //배열 요소의 개수를 알 수 있다.

    for(i=0; i<count; i++){
        
    }

}